# Ignore all files without extension (executables)
*      # Ignores ALL files
!*/    # But don't ignore directories 
!*.*   # And don't ignore files with extensions

# ignore specific files and directories
/server
/.server.pid
/snippetbox_data/
